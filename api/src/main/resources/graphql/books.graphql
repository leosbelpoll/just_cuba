schema {
    query: Query
}

type Query {
    books: [Book]
    book(id: String): Book
}

type Book {
    id: String
    title: String
    publisher: String
    authors: [String]
    publishedDate: String
}
